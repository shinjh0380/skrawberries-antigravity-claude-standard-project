---
direction: "ag-to-cc"
source-tool: "antigravity"
target-tool: "claude-code"
timestamp: "{ISO8601}"
task-id: "{task-id}"
status: "pending"
priority: "{priority}"
risk-level: "low"
evidence-path: "{evidence-path}"
---

# UI Feedback Request: {컴포넌트/페이지명}

## Summary

(어떤 UI 요소에 대한 피드백인지 요약)

## Visual Artifacts

**첨부 파일:**
- `{파일경로}` — {설명: 현재 상태 스크린샷}
- `{파일경로}` — {설명: 목표 상태 목업}
- `{파일경로}` — {설명: 녹화 영상}

## Current State

(현재 UI의 상태 설명)

## Desired State

(달성해야 할 UI 상태 설명)

## Specific Changes Required

| # | 요소 | 현재 값 | 목표 값 | 우선순위 |
|---|------|---------|---------|----------|
| 1 | (컴포넌트명) | (현재) | (목표) | high |
| 2 | | | | normal |

## Context

**관련 파일:**
- `{파일경로}` — (관련 컴포넌트/스타일)

**디자인 시스템:**
- (사용 중인 디자인 토큰, CSS 변수, Tailwind 클래스 등)

## Notes

(추가 제약사항, 접근성 요구사항, 브라우저 호환성 등)

## Normalization Instructions

이 문서는 `/ui-spec-normalize` 스킬로 처리되어 텍스트 스펙으로 변환됩니다.
